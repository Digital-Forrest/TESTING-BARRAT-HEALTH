[install]
# Allow lockfile updates during deployment
frozen-lockfile = false
# Use faster install method
cache = true
# Ensure consistent installs
exact = true
# Use binary lockfile format
lockfile = "bun.lockb"
